# 练习评分设计说明（v1.1）

> 面向学员与带教老师：**系统会给出 0–100 的练习参考分**，同时保留「建议性」免责声明。  
> 本说明解释「分数怎么来的」「和通过线什么关系」，避免「只有建议、没有打分」的误解。

---

## 1. 你会看到什么

练习结束后，在 **练习反馈** 页可看到：

| 展示项 | 含义 |
|--------|------|
| **练习参考分 X/100** | 加权汇总后的主分数（醒目展示） |
| **等级标签** | 如「优秀 / 达标 / 需改进 / 未达标（练习参考）」 |
| **维度得分条** | 必达核查、过程安排、沟通方式等分项 |
| **清单线** | L1 必达项是否全部 pass，且无红线违规 |
| **综合建议** | 参考分 ≥ 达标线 + 清单线过 + 无红线 → 综合达标 |
| **分项表格** | 每条检查项 pass / uncertain / fail 及证据摘录 |

**重要：** 分数与「建议通过/需改进」均为 **教学模拟参考**，不构成伦理审查、监管合规或个人能力认证结论。

---

## 2. 两套判定：清单线 + 参考分

### 2.1 清单线（硬性核对）

- **L1 必达项** 须全部为 `pass`（不含 skipped）
- **红线项**（如 IC-X、FU-X）任一 `fail` → 红线维度不通过
- 满足以上 → **清单线达到**

清单线对应「该场景最低核查是否做到」，偏 **合规核对**。

### 2.2 练习参考分（0–100）

在清单之外，用 **加权维度分** 反映过程质量：

- 每条启用项有 **权重 weight**
-  verdict 换算：`pass=1.0`，`uncertain=0.5`，`fail=0.0`
- 维度内得分 = 100 ×（ earned / max ）
- **总分** = 各维度得分 × 维度权重 的加权平均

**默认达标线：60 分**

### 2.3 综合达标（页面「综合建议」）

同时满足：

1. 红线维度通过（无 hardFail 项 fail）
2. 练习参考分 ≥ 60
3. 清单线达到

→ 显示 **综合建议：达标**；否则 **需改进**。

---

## 3. 场景与维度权重

### S1 知情同意（`scoring-rubric.json`）

| 维度 | 权重 | 说明 |
|------|------|------|
| 红线 | 一票否决 | IC-X 禁止施压、夸大疗效等 |
| L1 必达内容 | 50% | 目的、流程、风险、退出权等 |
| L2 过程合规 | 20% | 伦理批准、副本、再告知等 |
| 关切回应 | 30% | 对退出/疗效/副作用等关切的回应 |

### S5 随访 · 依从性与 AE（`scoring-rubric-followup.json`）

| 维度 | 权重 | 说明 |
|------|------|------|
| 红线 | 一票否决 | 禁止责备恐吓、禁止自行决定医学处理 |
| L1 必达核查 | 55% | 漏服、AE、合并用药、药盒、日记、家庭血压 |
| L2 过程与安排 | 25% | AE 联系、漏服处理原则、下次随访 |
| 沟通方式 | 20% | 平和鼓励、具体化提问 |

高血压案例 **CASE-HTN-PH3-W4-001** 使用随访量表。

---

## 4. 分数怎么算（示例）

假设某维度 3 条项，权重均为 1.0，结果 2 pass + 1 uncertain：

- earned = 1.0 + 1.0 + 0.5 = 2.5  
- max = 3.0  
- **维度分** = round(100 × 2.5 / 3) = **83**

总分再把各维度按上表权重加权。红线任一项 fail 时，综合建议通常为 **未达标**，即使其他维度较高。

---

## 5. 评分引擎组成

1. **规则层**：关键词/禁止语启发式（快速、可解释）
2. **LLM 语义层**（Agnes 可用时）：对 pass 标准做语义判断，输出 verdict + 证据句
3. **合并策略**：LLM 与规则冲突时偏保守；无 LLM 时仅规则
4. **数值汇总**：`compute_numeric_scores()` 生成 `scores` 对象写入反馈

数据文件：

- `web/data/scoring-rubric.json` — 知情同意  
- `web/data/scoring-rubric-followup.json` — 随访/依从性  
- `web/data/rubric-layperson.json` — 外行可读标题与说明  

---

## 6. 与旧版文档的差异

早期 `评分引擎设计文档.md` 写「一期不做百分制」。**v1.1 已启用 0–100 练习参考分**，该旧表述以本文为准。

---

## 7. 使用建议

- **学员**：先看总分与维度条，再点开 fail/uncertain 行对照证据改进  
- **带教**：综合建议 + 清单线 + 对话回放，人工复核仍为最终依据  
- **管理员**：调整 `scoringModel.dimensions` 权重或 `passScore` 即可改达标线，无需改代码  

---

*文档版本：2026-08-27 · 对应 rubric meta scoringModel v1.1*
