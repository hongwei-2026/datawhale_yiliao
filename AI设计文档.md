# AI 设计文档（模拟真实情况 · 标准化病人）

> 本文档定义如何用 AI **模拟临床试验受试者**，在对话中还原「真实沟通情境」，并保证**病情事实不被编造**。  
> 依据：[`病例设计文档.md`](./病例设计文档.md)、[`项目架构设计文档.md`](./项目架构设计文档.md)  
> 模型接入：Agnes（OpenAI 兼容，`agnes-2.0-flash`）；配置见 `.env` / [`数据库验证文档.md`](./数据库验证文档.md)  
> 落库：[`数据库设计文档.md`](./数据库设计文档.md)（`ai_*` / `session_message` / `case_*`）

---

## 1. 定位与目标

AI 在本项目中的主角色是 **标准化病人（SP）**，不是通用聊天机器人。

| 目标 | 说明 |
|------|------|
| 演得对 | 适应症、核心症状、用药与风险要点与病例事实包一致 |
| 像真人沟通 | 口语、含糊、担心、反复确认；可有理解偏差 |
| 可训练 | 支撑「进入案例 → 沟通 → 关键问题 → 结束」 |
| 可约束 | 库外医学细节不得编造；违规生成可拦截或回退 |
| 可审计 | 每次生成记录模型、Prompt 版本、耗时、grounding 结果 |

**非目标（一期）**：多模态形象、老人音色、情绪 TTS、写尽所有问答剧本、替代真实知情同意。

### 1.1 定位声明（重要）

本 AI 只是**训练用模拟受试者**，用于营造接近真实的练习情境。

| 请这样理解 | 请不要这样理解 |
|------------|----------------|
| 帮助在安全环境里练问法、练表达 | AI 说的、演的就等于真实病人 |
| 暴露常见沟通难点与情绪反应 | 模拟过关 = 真实场景已经合格 |
| 为带教讨论提供素材 | 可替代与真实受试者 / 家属打交道 |

**与人打交道的真实能力，只能在真实场景中培养。**  
系统给出的对话体验与后续评分反馈，均为**建议与练习参考**，存在疏漏或误判可能；最终提升仍靠临床现场实践、反思与带教指导。

与 **评分引擎** 的边界：AI 患者负责「真实情境下的应答模拟」；评分引擎负责「对照规范给出练习建议」。二者分离；且**两者的输出都不是最终裁定**。

---

## 2. 设计原则

| 原则 | 说明 |
|------|------|
| 事实层优先于情绪层 | 先保证病症与细节正确，再谈语气、焦虑、音色 |
| 不枚举剧本 | 结构化事实 + 人设槽位 + LLM 生成 |
| 不清楚就说不清 | 禁止 invent；回答「记不清 / 要问医生」 |
| 场景 × 人设可组合 | 换场景模板或改槽位，不重写整本剧本 |
| 生成后校验 | grounding 检查禁止编造与矛盾，失败则重生或回退 |
| 传统数据 + 智能体 | 病情用库表/JSON 约束；表达用 LLM |

---

## 3. 分层架构（模拟真实情况的核心）

```text
┌──────────────────────────────────────────┐
│ 表现层（后期） 语音、情绪起伏、停顿音效      │
└────────────────────┬─────────────────────┘
                     │
┌────────────────────▼─────────────────────┐
│ 生成层（LLM） 口语化回复、提问、犹豫        │
└────────────────────┬─────────────────────┘
                     │ 必须服从
┌────────────────────▼─────────────────────┐
│ 槽位层（人设） 理解力、焦虑、依从、表达习惯   │
└────────────────────┬─────────────────────┘
                     │ 必须服从
┌────────────────────▼─────────────────────┐
│ 事实层（不可编）症状/用药/风险/禁止编造 ★   │
└──────────────────────────────────────────┘
```

**真实情况**在本项目中的最低标准：事实层正确 + 槽位导致的合理沟通差异；不是戏剧冲突越多越真实。

一期：1 个病例（`CASE-T2DM-PH2-001` 占位）× 1 个基本人设 × 场景 S1（知情同意）。

---

## 4. 运行时数据流

```text
开练
  → 加载 case_package（锁定 version）+ persona + scene
  → 组装 patient_system Prompt（注入事实 JSON 摘要 + 禁止编造 + 槽位）
  → 写入 training_session

学员发言
  → 追加 session_message(role=trainee)
  → ai_agent_run(patient_reply)
  → LLM 生成候选回复
  → grounding_check
        pass → session_message(role=patient) + generation_log
        fail → regenerate（有限次）或 remediating_reply / 「记不清」

结束沟通
  → 交由评分引擎（见《评分引擎设计文档》）
```

---

## 5. Prompt 设计

### 5.1 模板分工

| template_code | purpose | 作用 |
|---------------|---------|------|
| `patient-system` | `patient_system` | 身份、事实约束、禁止项、槽位、场景目标 |
| `patient-turn` | `patient_turn` | 单轮：历史对话 + 最新学员话 + 输出要求 |
| （可选）`safety` | `safety` | 二次检查是否出现疗效保证等「替研究者说话」 |

版本进入 `ai_prompt_version`；会话锁定 `patient_prompt_version_id`。

### 5.2 System 必含块

1. **角色**：你是临床试验受试者/参与者，不是医生，不替研究者做知情同意宣教。  
2. **事实摘要**：主诉、核心症状（present=true）、合并用药概要、必须可知的风险白话（患者「听说过/担心」的程度按病例）。  
3. **禁止编造列表**：直接粘贴 `forbidden_fabrications` + `present=false` 的症状。  
4. **人设槽位**：年龄段、理解力、情绪、是否反复问「能否退出」。  
5. **场景**：S1 下患者目标是了解试验并决定是否同意，可提问、可犹豫。  
6. **输出**：短句口语；一次不必说完所有病史；不知则明确说不知。

### 5.3 温度与长度

| 用途 | temperature | max_tokens 建议 |
|------|-------------|-----------------|
| 患者回复 | 0.6–0.85 | 150–400 |
| 评分（另一文档） | 0.1–0.3 | 按项批量 |

---

## 6. 事实约束与 Grounding

### 6.1 硬约束来源

| 来源 | 用途 |
|------|------|
| `case_symptom` present/core | 可承认 / 应表现出的症状 |
| `case_risk_point` | 患者可表达的担心点（不等同于替医生宣教） |
| `case_forbidden` | 禁止出现的诊断/事件/承诺 |
| `medication` | 当前用药说法 |
| `remediating_reply` | 校验失败时的安全回退话术 |

### 6.2 校验类型（`ai_grounding_check`）

| check_type | 检测 |
|------------|------|
| `forbidden_symptom` | 出现未登记或 present=false 的核心矛盾症状 |
| `forbidden_claim` | 患者替试验打包票疗效/绝对安全 |
| `forbidden_event` | 编造心梗、肿瘤等未登记事件 |
| `contradiction` | 与本会话先前陈述矛盾（轻量：关键症状忽有忽无） |
| `role_break` | 变成医生/开始念 GCP 条款 |

### 6.3 失败策略

```text
第 1 次 fail → 带错误说明重生（regenerate）
第 2 次 fail → 使用 remediating_reply 或通用「这个我不太清楚，得问医生」
记录 action=regenerate | replace_with_fallback | block
绝不静默放行明显编造
```

---

## 7. 人设槽位如何影响「真实感」

一期基本患者（示例：`PER-ELDER-BASIC-01`）：

| 槽位 | 对生成的影响 |
|------|----------------|
| `cognition_level=average` | 能听懂大白话，术语需研究者解释时表示听不懂 |
| `emotion_baseline=anxious_mild` | 听到副作用会紧张，但不会崩溃离场 |
| `comprehension_style=repeats_key_questions` | 反复确认「能不能退出」「是不是一定有效」 |
| `concealment_tendency=low` | 被问到的已有症状如实说，不主动隐瞒 |
| `adherence_tendency=mostly_adherent` | S1 较少涉及；为后续用药场景预留 |

**后期扩展**：只改槽位即可模拟「爱隐瞒」「理解力弱」「家属抢话」等，无需新剧本库。

---

## 8. 场景行为（一期 S1）

| 学员行为 | 患者合理反应 |
|----------|----------------|
| 讲清目的与风险 | 追问、复述担心、考虑是否签字 |
| 催促签字 / 保证疗效 | 不安、质疑，或「那我再想想」 |
| 询问症状 | 按 fact 回答；未问及可不主动倾倒全部病史 |
| 问库外检查数值 | 「具体数字记不清」 |
| 闲聊无关 | 短暂回应后回到「这药/这试验到底安不安全」 |

关键问题（产品意义上的）= 影响同意决定与信任的问题（退出权、风险、是否一定有效等），患者应能提出或回应，而不是背诵标准答案。

---

## 9. 与 Agnes 的接入约定

| 项 | 约定 |
|----|------|
| Base URL | `https://apihub.agnes-ai.com/v1` |
| Model | `agnes-2.0-flash`（可配置） |
| 鉴权 | `Authorization: Bearer`；密钥仅 `.env` |
| 客户端 | `server/app/agnes.py` |
| 配置档 | `ai_model_profile.profile_code=agnes-patient-default` |

连通性验证：网站「数据库」页或 `POST /api/ai/ping`（写 `ai_generation_log`）。

正式患者对话 API（实现提纲）：

| 方法 | 路径 | 说明 |
|------|------|------|
| `POST` | `/api/sessions` | 创建会话（case + persona + scene） |
| `POST` | `/api/sessions/{id}/turns` | 学员发言 → 返回患者回复 |
| `POST` | `/api/sessions/{id}/complete` | 结束并触发评分引擎 |

---

## 10. 与评分引擎的协作

| 环节 | AI 患者 | 评分引擎 |
|------|---------|----------|
| 对话中 | 生成符合事实的回复 | 不评分（一期） |
| 对话后 | 不再改写历史消息 | 整段对照 Rubric |
| 病未就绪 | 仍按占位事实演 | 症状硬评 skipped |
| 日志 | `purpose=patient_*` | `purpose=scorer/feedback` |

评分不得因「患者太难搞」而放宽 L1 练习通过线；人设难度通过场景说明与教员配置体现。  
同时须在产品文案中声明：**练习通过 ≠ 真实能力合格**；建议学员将报告交带教讨论，并在真实沟通中继续验证。

---

## 11. 一期范围与演进

### 必做

- 单病例 + 单人设 + S1  
- System 注入事实与禁止编造  
- Grounding 最小集（禁止编造 + 角色崩坏）  
- 全链路写 `session_message` + `ai_generation_log`  

### 明确不做（一期）

- 语音 / 多模态形象  
- 多患者状态大规模组合  
- 随访全时点自动切换  
- 患者侧实时「教学提示」（避免剧透评分）  

### 二期方向

- 槽位组合包（隐瞒、低理解力等）  
- S3/S5 用药与随访场景行为策略  
- 更强矛盾检测与症状时间线  
- 可选语音表现层  

---

## 12. 质量评审清单

- [ ] 核心症状能否被问出来且说法自然？  
- [ ] 追问库外内容时是否拒绝编造？  
- [ ] 是否出现替研究者保证疗效？  
- [ ] 人设「反复确认退出」是否可稳定复现？  
- [ ] 每次回复是否有 generation_id 与 grounding 记录？  
- [ ] 占位病例是否标注，避免当真实方案外传？  
- [ ] 产品/报告是否声明：模拟与评分仅为建议，最终靠真实场景与人对人实践？  

---

## 13. 实现顺序建议

1. 固化 `patient-system` / `patient-turn` 模板与 JSON 注入器  
2. `POST /turns` + grounding 最小实现  
3. 用固定学员话术做回归（症状一致性、禁止编造）  
4. 与评分引擎 `complete` 打通闭环  
5. 正式病例替换后回归事实层  

---

## 修订记录

| 日期 | 说明 |
|------|------|
| 2026-08-23 | 初稿：分层架构、Prompt、grounding、人设、S1 行为、Agnes 接入与评分边界 |
| 2026-08-23 | 明确：AI 模拟与评分均为建议；真正提升靠真实场景与人对人实践 |
