# 数据库与 Agnes AI 接入验证文档

> 证明两件事：① **数据库已真实创建并灌入标准/病例数据**；② **Agnes API 已接通，且调用记录写入数据库**。  
> 密钥保存在项目根目录 `.env`（已加入 `.gitignore`，勿提交仓库）。

---

## 1. 推荐展示入口（外行友好）

**请只开一个服务**（网站 + 数据库 API 已合并）：

```powershell
cd d:\datawhale
python -m uvicorn server.app.main:app --host 127.0.0.1 --port 8000
```

| 用途 | 地址 |
|------|------|
| **网站首页** | http://127.0.0.1:8000/ |
| **模拟对话** | http://127.0.0.1:8000/#practice |
| **练习反馈** | http://127.0.0.1:8000/#feedback |
| **数据库展示页** | http://127.0.0.1:8000/#database |
| 入门导读 | http://127.0.0.1:8000/#guide |
| 数据库验证 API | http://127.0.0.1:8000/api/db/verify |
| Agnes 连通 + 写库 | `POST` http://127.0.0.1:8000/api/ai/ping |
| 健康检查 | http://127.0.0.1:8000/health |
| OpenAPI | http://127.0.0.1:8000/docs |

在「数据库」页用白话展示：

- 仓库分 5 组货架（标准 / 评分 / 病例 / 练习记录 / AI）
- 每张表的中文含义 + 当前条数
- 样例：默认病例、核心症状、规范文件
- 一键：刷新 / 试写记录 / 呼叫 Agnes

**数据库文件**：

```text
d:\datawhale\data\sp_training.db
```

正式生产库仍以 [`db/schema.sql`](./db/schema.sql)（PostgreSQL）为准；演示用 SQLite。

---

## 2. 给外来人看的 30 秒话术

1. 打开首页，先看「入门导读」——这是练研究者怎么跟受试者说话。  
2. 点「数据库」——仓库里已经装了标准和病例（数字 > 0）。  
3. 点「呼叫 Agnes 并写库」——AI 回话，下方日志多一条，说明真接上了。

---

## 3. 判定清单

| # | 检查项 | 如何证明 |
|---|--------|----------|
| 1 | 网站能开 | http://127.0.0.1:8000/ |
| 2 | 库文件存在 | `data/sp_training.db` |
| 3 | 标准已入库 | 「数据库」页 · 规范文件条数 ≥ 1 |
| 4 | 病例已入库 | 默认病例 + 核心症状列表 |
| 5 | 可写库 | 「试写一条记录」后探针增加 |
| 6 | Agnes 接通 | 「呼叫 Agnes」有中文回复 |
| 7 | AI 落库 | 「最近 AI 通话记录」出现新行 |

---

## 4. 配置

见 [`.env.example`](./.env.example)。真实密钥只放 `.env`。

---

## 修订记录

| 日期 | 说明 |
|------|------|
| 2026-08-23 | 初版验证地址 |
| 2026-08-23 | 网站合并入口；`#database` 外行展示页 |
