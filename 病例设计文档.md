# 病例设计文档（底座 B）

> 本文档定义 AI 标准化病人系统中 **病例 / 症状事实包** 的设计原则、包结构与一期占位病例。  
> 配套：[`数据库设计文档.md`](./数据库设计文档.md)、[`项目架构设计文档.md`](./项目架构设计文档.md)、[`标准设计文档.md`](./标准设计文档.md)  
> 运行时数据：[`web/data/cases/`](./web/data/cases/)、索引 [`web/data/cases-index.json`](./web/data/cases-index.json)

---

## 1. 定位：底座 B 做什么

训练闭环要同时成立两件事：

| 底座 | 保证什么 | 若失败 |
|------|----------|--------|
| A 标准与评分 | 「说得对不对」有尺子 | 反馈乱指，变不了专业 |
| **B 病情真实** | 「演的是这个病的人」 | 对着假病人练，训练作废 |

病例不是剧本，而是 **约束 AI 患者说话的事实库**：症状、用药、风险、随访节点写在库里；库里没有的医学细节，AI **不得编造**。

一期目标：**1 个病 × 1 个基本患者 × 1 个主场景（知情同意 S1）**，内容可先用占位数据，但 **字段与表结构一次设计到位**。

---

## 2. 设计原则

| 原则 | 说明 |
|------|------|
| 事实优先于情绪 | 先保证适应症、核心症状、用药与风险点正确；情绪/音色后挂 |
| 不枚举剧本 | 用结构化事实 + 人设槽位生成对话，不写尽所有问答 |
| 分层可替换 | 疾病目录、试验方案摘要、病例实例、人设可分开升级 |
| 版本可追溯 | 病例包带 `version` / `data_status`；正式数据替换占位时升版本 |
| 与评分咬合 | 可追问的症状、风险点有稳定 ID，供评分项 `binds_symptom` 绑定 |
| 占位可运行 | 占位病例也必须结构完整、自洽，便于联调；标注 `placeholder`，正式数据到位后整包替换 |

### 2.1 什么可以编、什么不能编

| 可生成（表现层） | 不可编造（事实层） |
|------------------|-------------------|
| 口语化、停顿、跑题、情绪起伏 | 未登记的症状、检查结果、诊断 |
| 记不清、说不清、反复追问 | 未登记的合并用药、过敏史 |
| 对风险的担心、犹豫签同意 | 未在风险表中的 AE/疗效承诺 |

规则：**不清楚时表现为「记不清 / 医生没跟我说清」**，而不是 invent 医学细节。

---

## 3. 病例包分层（逻辑模型）

```text
Disease（疾病目录）
    └── TrialProtocolSummary（试验方案摘要，可跨病例复用）
            └── CasePackage（病例实例 = 训练用「这一例」）
                    ├── SymptomFact[]          ★ 核心
                    ├── MedicationProfile
                    ├── RiskPoint[] / BenefitPoint[]
                    ├── VisitSchedule[]
                    ├── Contraindication / ForbiddenFabrication
                    ├── PatientPersona（一期 1 个基本患者）
                    └── SceneBinding[]（挂到 S1/S2…）
```

| 层 | 职责 | 一期 |
|----|------|------|
| Disease | 适应症编码、标准病名、典型症状模板参考 | 1 条 |
| TrialProtocolSummary | 试验阶段、干预概要、已知风险类别 | 1 条（可简化） |
| CasePackage | 可开练的「这一例」：病程、主诉、具体事实 | **1 包占位** |
| PatientPersona | 年龄段、理解力、依从倾向等槽位 | 1 个 |
| SceneBinding | 本病例在哪些场景启用、用哪套评分子集 | 先绑 S1 |

---

## 4. 病例包字段约定（业务视角）

完整表结构见 [`数据库设计文档.md`](./数据库设计文档.md)。此处按「写病例的人」需要关心的块说明。

### 4.1 包头 `meta`

| 字段 | 含义 |
|------|------|
| `case_id` | 稳定业务主键，如 `CASE-T2DM-PH2-001` |
| `title` / `short_title` | 展示名 |
| `data_status` | `placeholder` \| `draft` \| `reviewed` \| `active` \| `deprecated` |
| `version` | 语义化版本，如 `0.1.0`（占位）→ `1.0.0`（正式） |
| `locale` | 默认 `zh-CN` |
| `disclaimer` | 教学模拟声明 |

### 4.2 病情摘要 `clinical_summary`

适应症、病程阶段、主诉、现病史要点、既往史要点、家族史（可选）。  
**占位阶段**：写清「模拟用、待临床专家替换」即可，但条目要自洽。

### 4.3 症状事实 `symptoms[]`（★ 最重要）

每条症状建议包含：

| 字段 | 作用 |
|------|------|
| `symptom_id` | 稳定 ID，供评分绑定 |
| `name` / `clinical_name` | 患者说法 / 临床名 |
| `category` | 如 constitutional / metabolic / ocular… |
| `is_core` | 核心症状=必须演对 |
| `present` | 本病例是否当前存在 |
| `severity_band` | mild / moderate / severe |
| `onset` / `course` | 起病与演变（结构化短句） |
| `patient_may_say[]` | 含糊口述变体 |
| `researcher_should_ask[]` | 研究者宜追问的点（供后期评分） |
| `forbidden_confusions[]` | 易与他病混淆、禁止 AI 串戏的说法 |

### 4.4 用药 `medication`

试验药（或对照）用法概要、合并用药、常见不依从、注意事项。  
知情同意场景下可偏简；用药/随访场景再加厚。

### 4.5 风险与获益不确定性 `risks[]` / `benefits[]`

与 ICF/研究者手册对齐的「应告知要点」，**不是**疗效保证。  
每条带 `risk_id`，便于评分项 IC-C04 等引用。

### 4.6 访视 `visits[]`

访视编码、时点、需采集的症状/AE 项。一期可 2–3 个节点占位。

### 4.7 禁止编造 `forbidden_fabrications[]`

明确列出「本病例不得出现」的症状、诊断、检查结果，作为生成护栏。

### 4.8 人设槽位 `persona`

枚举型槽位（年龄段、认知、情绪基线、依从、隐瞒倾向、表达习惯），**不是长篇人设小说**。

### 4.9 场景绑定 `scene_bindings[]`

例如：`S1 informed_consent` → 启用本包风险/获益摘要 + 基本病情；`S3 medication` 待二期启用。

---

## 5. 一期占位病例：`CASE-T2DM-PH2-001`

### 5.1 为什么选 2 型糖尿病（T2DM）

| 理由 | 说明 |
|------|------|
| 常见、好理解 | 外行也能建立「慢病试药」语境 |
| 知情同意点齐全 | 风险、获益不确定、需长期随访、生活方式 |
| 症状可核验 | 多饮多尿、乏力、视物模糊等典型表现 |
| 易扩展 | 后续可加用药依从、低血糖担忧、合并用药 |

> **重要**：本包 `data_status = placeholder`。试验药名、剂量、具体 AE 发生率等均为 **教学虚构**，正式上线前须由临床/GCP 人员按真实方案替换。

### 5.2 一句话设定

> 68 岁男性，2 型糖尿病 8 年，血糖控制欠佳；拟参加一项口服降糖新药 II 期临床试验的知情同意沟通。患者听力尚可、理解力一般，会反复确认「能不能随时退出」「是不是一定能治好」。

### 5.3 核心症状（占位，须演对）

| ID | 名称 | 说明 |
|----|------|------|
| SX-T2DM-01 | 口渴多饮 | 近半年加重 |
| SX-T2DM-02 | 尿频多尿 | 夜尿增多 |
| SX-T2DM-03 | 乏力 | 活动后明显 |
| SX-T2DM-04 | 视物模糊 | 间歇出现 |
| SX-T2DM-05 | 肢体麻木 | 足趾偶发麻木（轻） |

### 5.4 明确不演成的（禁止串戏）

- 不演成 1 型糖尿病急症酮症表现（除非正式数据改写）  
- 不编造肿瘤、心梗住院等未登记事件  
- 不声称「试验药保证血糖正常 / 无副作用」

### 5.5 与评分咬合（当前策略）

| 状态 | 策略 |
|------|------|
| 底座 A 知情同意 L1 | **不依赖**具体症状追问即可硬评 |
| 症状追问类占位项（RK/MD/FU） | 本包提供 `symptom_id`，但评分项仍 **disabled**，待正式数据评审后再启用硬评 |

---

## 6. AI 患者如何使用本包

```text
会话开始
  → 加载 CasePackage（按 case_id + version）
  → 注入系统约束：
       · 只能陈述 present=true 的症状事实
       · 风险/获益只能复述 risks/benefits 表内要点（口语化）
       · 命中 forbidden_fabrications → 改为「记不清 / 要问医生」
  → 叠加 persona 槽位调整语气与理解表现
  → 生成回复
```

**一致性检查（建议工程实现）**：

1. 生成后抽检：是否出现库外症状名；  
2. 同会话前后矛盾检测（如先说无麻木后又说长期麻木）；  
3. 病例版本写入 `Session.case_version`，反馈可复现。

---

## 7. 正式数据替换流程

1. 临床/教研提供真实适应症要点、ICF 风险摘要、症状清单；  
2. 按本结构填入新版本（建议 `1.0.0`），`data_status → reviewed`；  
3. 专家走查：核心症状、禁止编造、风险表述；  
4. `data_status → active`，旧占位 `deprecated` 但保留只读；  
5. 开启依赖 `symptom_id` 的评分项；跑一轮回归对话。

**不要**在占位结构上「打补丁改字段名」；缺字段应先改 [`数据库设计文档.md`](./数据库设计文档.md) 与 schema，再改 JSON。

---

## 8. 文件与维护

| 路径 | 说明 |
|------|------|
| `web/data/cases-index.json` | 病例目录（列表、状态、默认病例） |
| `web/data/cases/CASE-T2DM-PH2-001.json` | 一期占位完整包 |
| `db/schema.sql` | 关系库建表（**全项目**：标准 / 病例 / 会话 / AI / 反馈） |
| [`数据库设计文档.md`](./数据库设计文档.md) | 全库 ER、模块、字段与 JSON 映射 |
| 本文档 | 病例业务设计与写作规范 |

JSON 用于一期配置与前端展示；上线后同一模型落入数据库，JSON 可作为导入导出格式。

---

## 9. 评审清单（病例包）

- [ ] `case_id` / `symptom_id` / `risk_id` 是否稳定、可被评分引用？  
- [ ] 核心症状是否都有 `patient_may_say`？  
- [ ] `forbidden_fabrications` 是否覆盖易串戏点？  
- [ ] 风险表述是否避免疗效保证？  
- [ ] `data_status` 是否正确（占位不得标 active）？  
- [ ] 人设是否仅为槽位枚举，而非不可维护长剧本？  
- [ ] 是否声明教学模拟、非真实受试者？

---

## 修订记录

| 日期 | 说明 |
|------|------|
| 2026-08-23 | 初稿：病例分层、占位病例 CASE-T2DM-PH2-001、与评分咬合策略 |
