# 评分引擎设计文档（评分 + 反馈）

> 本文档定义训练结束后如何**对照评分清单判分**，以及如何生成**可执行、可追溯的反馈**。  
> 依据：[`标准设计文档.md`](./标准设计文档.md)、[`项目架构设计文档.md`](./项目架构设计文档.md)  
> 数据落库：[`数据库设计文档.md`](./数据库设计文档.md)（`rubric_*` / `score_result` / `feedback_report`）  
> 运行时评分表：[`web/data/scoring-rubric.json`](./web/data/scoring-rubric.json)

---

## 1. 定位与目标

评分引擎服务于闭环最后一环：

```text
进入案例 → 与受试者沟通 → 处理关键问题 → 完成沟通 → 【评分 + 反馈】
```

| 目标 | 说明 |
|------|------|
| 评得准（尽力） | 依据权威规范抽取的清单，尽量可解释；但仍可能误判，输出按建议呈现 |
| 说得清 | 每条未过项尽量指出对话证据与规范来源 |
| 能改进 | 反馈给 2–5 条**可参考的**练习建议（非强制命令） |
| 可复现 | 锁定评分表版本、病例版本、所用模型/Prompt 版本 |
| 保持谦逊 | 界面与文案明确「建议 / 供参考」，避免绝对化合格话术 |

**非目标（一期）**：替代伦理审查、出具监管合规结论、句级实时打分、百分制细抠共情。

### 1.1 定位声明（重要）

| 本系统是 | 本系统不是 |
|----------|------------|
| 教学训练中的 **建议与复盘辅助** | 对沟通能力的最终裁定 |
| 帮助发现可能的遗漏与风险话术 | 可替代带教老师 / 伦理审查的结论 |
| 为真实上岗前提供练习场 | 真实场景与人对人沟通本身 |

**AI 与系统给出的评分、反馈不一定正确或完整。**  
学员的真正提升，最终仍取决于在**真实场景里与人打交道**的实践、反思与带教指导。本系统输出一律按 **「建议」** 呈现，不得表述为「已合格 / 已合规 / 已认证」。

**免责声明（必须出现在报告中）**：本结果为**教学模拟建议**，不构成正式授权、合规认证或对个人能力的最终评价；请结合带教与真实实践理解。

---

## 2. 设计原则

| 原则 | 说明 |
|------|------|
| 整段会话对照清单 | 不以每一句为评分单位，降低噪声、便于解释 |
| L1 优先 | 一期以硬性必达 / 禁止为主；L3 主观质量不作为通过门槛 |
| 双层判定 | 规则层抓明显违规与关键词覆盖；LLM 层做语义复核与证据抽取 |
| 证据必引 | `fail` / `uncertain` 必须带对话片段（turn + quote） |
| 与病例解耦开关 | 依赖症状的评分项仅在病例 `reviewed/active` 且 binding.enabled 时硬评 |
| 传统算法 + 智能体分工 | 汇总通过线、分层统计用确定性算法；语义理解与建议生成用 LLM |

---

## 3. 在系统中的位置

```text
                    ┌─────────────────────┐
                    │  评分对照表 Rubric    │  ← 底座 A
                    └──────────┬──────────┘
                               │
会话结束 ──► 评分引擎 ──► score_result[] ──► 反馈生成 ──► feedback_report
               │                                    ▲
               │ 可选引用                            │
               ▼                                    │
         病例事实 / binding                   改进建议文案
         （底座 B，仅启用项）
```

与 **AI 患者** 的边界：患者负责「演得像（仍是模拟）」；评分引擎负责「给出对照规范的**练习建议**」。二者可共用同一 Agnes 接入，但 **Prompt、温度、审计日志分开**。

**系统评分与 AI 表现都不能当作最终真理**：模拟再像也不等于真实受试者；分数再完整也不替代带教与现场实践。


---

## 4. 评分对象与通过规则

### 4.1 输入

| 输入 | 来源 |
|------|------|
| 完整对话 | `session_message`（role + content + turn_index） |
| 场景 | 如 S1 `informed_consent` |
| 评分集 | `rubric_set` + `enabled_mvp=true` 的 `rubric_item` |
| 病例上下文（可选） | 风险点、症状绑定（仅启用项） |
| 版本锁 | `case_version`、`rubric_version`、`prompt_version` |

### 4.2 一期通过线（S1）

与评分表 meta 一致：

> **L1 全部 `pass`，且不存在任何 IC-X（禁止项）`fail`。**

| 结果 | 条件 |
|------|------|
| 通过 | 满足通过线 |
| 未通过 | 任一 L1 未过，或任一 hard_fail 项失败 |
| 存疑会话 | 存在 `uncertain` 且无 hard_fail；教员可复核（一期可先计为未通过并提示存疑） |

L2 计入报告与改进建议，**不单独否决**一期通过（可配置）。

---

## 5. 引擎流水线

```text
1. LoadRubric
     加载本场景 active 评分集；过滤 enabled_mvp
     过滤：binds_symptom 且 binding 未启用 → verdict=skipped

2. PreprocessTranscript
     拼接研究者侧文本 / 全文；切分 turns；可选脱敏

3. RulePass（确定性）
     禁止项启发式：过度承诺、胁迫话术模板等 → 候选 fail
     内容项关键词/要点词典 → 候选 pass/miss（弱证据）

4. LLMJudge（语义）
     按项或按组调用 scorer Prompt
     输出：item_code, verdict, evidence_spans[], comment, confidence

5. Merge
     hard_fail：规则或 LLM 任一确认 fail → fail
     冲突策略：规则 fail + LLM pass → uncertain（或配置为 fail）
     写入 score_result（scorer_type=rule|llm|hybrid）

6. Aggregate
     计算 overall_pass；分组统计 IC-P / IC-C / IC-X / L2

7. FeedbackGen
     基于未过项生成 improvements[]；组装 feedback_report
```

### 5.1 单条评分项输出契约

```json
{
  "item_code": "IC-C04",
  "verdict": "fail",
  "evidence_spans": [
    { "turn_index": 3, "role": "trainee", "quote": "这个药很安全，没什么事" }
  ],
  "comment": "未说明主要风险，反而保证安全",
  "source_refs": ["gcp-2020:第二十四条（七）"],
  "confidence": 0.82
}
```

落库字段对齐 `score_result`。

---

## 6. 规则层与 LLM 层分工

### 6.1 规则层适合做什么

| 类型 | 示例 |
|------|------|
| 禁止话术模板 | 「一定能治好」「绝对没副作用」「不签就不给你治」 |
| 明显缺失信号 | 全文未出现「退出/不愿意/风险/目的」等（仅作候选，需 LLM 复核） |
| 结构检查 | 对话轮次过少、研究者发言占比异常等质量门禁 |

规则层产出 **候选**，默认不单独对 L1 内容项一锤定音（禁止项可配置为强规则）。

### 6.2 LLM 层适合做什么

| 类型 | 示例 |
|------|------|
| 语义覆盖 | 是否用白话讲清目的、风险与获益不确定 |
| 证据抽取 | 精确引用学员原话 |
| 边界情况 | 「说了风险但严重淡化」→ fail 或 uncertain |
| 改进建议 | 针对未过项生成可练话术 |

### 6.3 Scorer Prompt 要求（摘要）

- 只依据给定对话与评分项 criteria，禁止引入库外医学知识当扣分依据  
- 必须输出结构化 JSON（可用 schema / tool calling）  
- 每条 fail 至少 1 段 evidence；无证据不得判 fail（改 uncertain）  
- 温度偏低（建议 0.1–0.3）；记录 `ai_generation_log`

---

## 7. 反馈系统设计

### 7.1 报告结构

| 字段 | 说明 |
|------|------|
| `overall_pass` | 是否达到**本期练习通过线**（教学门槛，非真实能力认证） |
| `summary` | 2–4 句总述；须含「以下为系统建议，供参考」语义 |
| `by_group` | 过程 / 内容 / 禁止 / L2 统计 |
| `items[]` | 与 `score_result` 一致的可读列表 |
| `improvements[]` | 2–5 条可参考的练习建议（对应未过项） |
| `disclaimer` | **必填**：教学模拟建议；非最终评价；请回到真实场景与带教 |
| `payload` | 完整快照 JSONB |

文案约束（反馈生成必须遵守）：

- 用「建议检查 / 可以练习 / 供参考」，不用「你已不合格且无法从事试验」等断语；  
- 明确：**真实沟通能力以临床现场与人对人实践为准**；  
- 可提示：将本报告带给带教老师讨论，而不是只信系统分数。

### 7.2 改进建议生成原则

1. **一条建议对应一类问题**（不要空泛「多共情」）  
2. **可执行**：给出可直接练习的说法要点，而非仅「请遵守 GCP」  
3. **引用规范短名**：如「对应知情同意须说明风险」  
4. **优先 L1 / IC-X**；L2 作为加练项放后  

示例：

> 你在对话中说「这个药很安全」。请改为同时说明：可能的不适、未知风险，以及「不一定对每个人有效」。依据：IC-C04 / IC-C05。

### 7.3 反馈 Prompt 输入

- 未过 / 存疑项列表（含 evidence）  
- 学员角色为「研究者」  
- 禁止生成虚假合规背书或「已具备独立知情同意资质」类表述  
- 必须保留「建议仅供参考，最终提升靠真实场景实践」类提示  

---

## 8. 与底座 B 的咬合

| 情况 | 引擎行为 |
|------|----------|
| 病例 `placeholder` / `draft` | 症状追问类 → `skipped`；IC 通用项照常评 |
| `reviewed` / `active` + binding.enabled | 可对绑定的 `SX-*` / `RK-*` 做覆盖检查 |
| AI 患者演错病 | **不属于评分引擎纠错范围**；应在患者侧 grounding 拦截。评分仍按对话事实评沟通合规 |

原则：**标准对、病假** 或 **病对、标准假** 都会毁掉训练；引擎不得在病未就绪时硬扣「没问到某症状」。

---

## 9. 接口与落库（实现提纲）

### 9.1 建议 API

| 方法 | 路径 | 说明 |
|------|------|------|
| `POST` | `/api/sessions/{id}/complete` | 结束会话并触发评分 |
| `GET` | `/api/sessions/{id}/score` | 读取 `score_result` |
| `GET` | `/api/sessions/{id}/feedback` | 读取 `feedback_report` |
| `POST` | `/api/sessions/{id}/rescore` | 教员触发重评（锁新 prompt 版本） |

### 9.2 写库顺序

1. `ai_agent_run(run_type=score_session)`  
2. 若干 `ai_generation_log` + 批量 `score_result`  
3. `ai_agent_run(run_type=feedback)` + `feedback_report`  
4. `training_session.status=completed`

---

## 10. 一期范围与非目标

### 必做

- S1 知情同意 MVP 清单（现有 JSON）整段评分  
- hybrid：禁止项规则 + LLM 语义  
- 反馈含 overall、分项、证据、建议、免责声明  
- 版本锁定与审计日志  

### 明确不做（一期）

- 句级实时打分条  
- 百分制 / 复杂权重排名  
- 多场景同时评分  
- 自动签发「合格证书」  

---

## 11. 质量与评审清单

- [ ] 通过线是否与 `scoring-rubric.meta.passRule` 一致？  
- [ ] fail 是否都有 evidence？  
- [ ] IC-X 失败是否必导致 overall 不通过？  
- [ ] 占位病例下症状硬评是否均为 skipped？  
- [ ] 报告是否含教学模拟免责声明，且未把结果写成最终能力认证？  
- [ ] 文案是否使用「建议 / 供参考」，避免绝对化合格/合规断语？  
- [ ] 是否提示真实场景与人对人实践、带教指导的重要性？  
- [ ] 重评是否保留历史 `score_result` 或版本化？（建议：新 run，旧结果可标记 superseded）

---

## 12. 实现顺序建议

1. 离线：用固定对话 fixture 跑规则层 + 人工核对 L1  
2. 接入 Agnes scorer Prompt，对齐 JSON 契约  
3. `complete` API + 落库  
4. 反馈生成与网站「评分结果」页  
5. 病例正式后启用 binding 项  

---

## 修订记录

| 日期 | 说明 |
|------|------|
| 2026-08-23 | 初稿：流水线、规则/LLM 分工、反馈结构、与病例咬合、API 提纲 |
| 2026-08-23 | 明确：系统评分仅为建议；最终提升靠真实场景与人对人实践 |
