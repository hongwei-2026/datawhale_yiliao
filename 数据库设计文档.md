# 数据库设计文档（全项目）

> **本文档是整个 AI 标准化病人系统的持久化设计**，覆盖底座 A（标准与评分）、底座 B（病例事实）、训练运行时、AI 调用与反馈存储。  
> 业务依据：[`标准设计文档.md`](./标准设计文档.md)、[`病例设计文档.md`](./病例设计文档.md)、[`项目架构设计文档.md`](./项目架构设计文档.md)  
> DDL：[`db/schema.sql`](./db/schema.sql)（PostgreSQL 14+）  
> 一期配置仍可用 `web/data/*.json`；表结构按上线目标一次设计到位。

---

## 0. 文档范围（此前缺什么）

上一版偏「病例库」，**未写完**。本版补全为全库：

| 域 | 是否纳入 | 说明 |
|----|----------|------|
| 标准注册 / 条款 / 术语 | ✅ | 底座 A 主数据 |
| 评分对照表 / 来源引用 / 与病例咬合 | ✅ | 底座 A 核心资产 |
| 场景目录 | ✅ | S1–S5 |
| 疾病 / 方案 / 病例 / 症状 / 风险 / 人设 | ✅ | 底座 B |
| 学员 / 训练会话 / 对话消息 | ✅ | 运行时 |
| 评分结果 / 反馈报告 | ✅ | 闭环输出 |
| AI 模型配置 / Prompt / 生成日志 / 事实校验 | ✅ | AI 数据存储 |
| 内容层（导读、白话解释） | ✅ | 可选，便于后台改文案 |
| 真实医院 HIS / 真实 PII | ❌ | 非目标 |

---

## 1. 设计目标与原则

| 目标 | 做法 |
|------|------|
| 双底座同库可联查 | 标准项与症状/风险通过 `rubric_item_binding` 咬合 |
| 业务码稳定 | `gcp-2020`、`IC-P04`、`CASE-*`、`SX-*` 等对外 ID 不随内部 UUID 变 |
| 版本可复现 | 会话锁定 `case_version`、`rubric_version`、所用 prompt/model 版本 |
| 配置与运行分离 | 主数据可评审；会话/日志只追加 |
| AI 可审计 | 每次生成可追溯：输入摘要、输出、是否触犯禁止编造、耗时与模型 |
| 教学安全 | 无真实受试者 PII；占位数据 `data_status` 可区分 |

**推荐引擎**：PostgreSQL 14+（JSONB）。本地验证用 SQLite（见 [`数据库验证文档.md`](./数据库验证文档.md)、`data/sp_training.db`）。

---

## 2. 逻辑分层与 ER 总览

```text
┌─────────────────────────────────────────────────────────────────┐
│  M0 公共：scene · app_user(可选) · data_import_log               │
├───────────────────────────────┬─────────────────────────────────┤
│  M1 底座 A 标准与评分          │  M2 底座 B 病例事实               │
│  standard_doc                 │  disease · trial_protocol         │
│  standard_category            │  case_package · case_symptom …    │
│  standard_clause              │  patient_persona · bindings       │
│  terminology                  │                                   │
│  rubric_set · rubric_item     │                                   │
│  rubric_item_source           │                                   │
│  rubric_item_binding ─────────┼──► symptom_code / risk_code       │
├───────────────────────────────┴─────────────────────────────────┤
│  M3 训练运行时                                                   │
│  training_session → session_message → score_result → feedback_report │
├─────────────────────────────────────────────────────────────────┤
│  M4 AI 数据存储                                                  │
│  ai_model_profile · ai_prompt_template · ai_prompt_version       │
│  ai_generation_log · ai_grounding_check · ai_agent_run           │
├─────────────────────────────────────────────────────────────────┤
│  M5 内容层（可选）                                               │
│  content_guide · content_standard_lay · content_rubric_lay       │
└─────────────────────────────────────────────────────────────────┘
```

**一次开练的数据路径**：

```text
学员选择 scene + case + persona
  → 加载标准评分集(rubric_set) + 病例事实包
  → 每轮对话：写 session_message + ai_generation_log（患者侧）
  → 结束：评分引擎写 score_result（可再记 AI 评分日志）
  → 生成 feedback_report
```

---

## 3. 模块与表清单

### M0 公共

| 表 | 说明 |
|----|------|
| `scene` | 训练场景 S1–S5 |
| `app_user` | 学员/教员账号（一期可空，用 `trainee_ref` 字符串） |
| `data_import_log` | JSON→库导入审计 |

### M1 底座 A — 标准与评分

| 表 | 说明 | 对齐 JSON |
|----|------|-----------|
| `standard_category` | 标准分类 | `standards-registry.categories` |
| `standard_doc` | 标准注册项（GCP/赫尔辛基等） | `standards-registry.standards[]` |
| `standard_link` | 官方/本地链接 | `links[]` / `localFile` |
| `standard_clause` | 关键条款摘要 | `keyClauses[]` |
| `terminology` | 统一术语 | `terminology.json` |
| `rubric_set` | 一套评分对照表（按场景+版本） | `scoring-rubric.meta` |
| `rubric_layer` | L1/L2/L3 定义 | `layers[]` |
| `rubric_item` | 评分项 | `items[]` |
| `rubric_item_source` | 评分项←规范条款引用 | `sources[]` |
| `rubric_item_binding` | 评分项←病例症状/风险 | 标准设计 §9 |

### M2 底座 B — 病例事实

| 表 | 说明 |
|----|------|
| `disease` | 疾病目录 |
| `trial_protocol` | 试验方案摘要 |
| `case_package` | 病例主表 |
| `case_symptom` | 症状事实 ★ |
| `case_medication` / `case_concomitant_drug` | 用药 |
| `case_risk_point` / `case_benefit_point` | 风险与获益不确定性 |
| `case_visit` / `case_visit_item` | 访视 |
| `case_forbidden` | 禁止编造 |
| `case_comorbidity` / `case_lab_hint` | 合并症 / 化验提示 |
| `patient_persona` / `case_persona_link` | 人设 |
| `case_scene_binding` | 病例×场景启用 |

### M3 训练运行时

| 表 | 说明 |
|----|------|
| `training_session` | 一次训练 |
| `session_message` | 对话轮次 |
| `score_result` | 逐项评分 |
| `feedback_report` | 结束后结构化反馈 |

### M4 AI 数据存储

| 表 | 说明 |
|----|------|
| `ai_model_profile` | 模型接入配置（不含明文密钥，密钥走密钥库/环境变量引用） |
| `ai_prompt_template` | Prompt 模板目录（患者 / 评分 / 反馈） |
| `ai_prompt_version` | 模板版本正文 |
| `ai_agent_run` | 一次 Agent 编排（可含多步） |
| `ai_generation_log` | 单次 LLM 调用明细 |
| `ai_grounding_check` | 事实校验结果（是否编造库外症状等） |

### M5 内容层（可选）

| 表 | 说明 | 对齐 |
|----|------|------|
| `content_guide_section` | 入门导读 | `project-guide.json` |
| `content_standard_lay` | 标准白话 | `standards-layperson.json` |
| `content_rubric_lay` | 评分白话 | `rubric-layperson.json` |

---

## 4. 关键表字段（按模块）

### 4.1 M1 标准

#### `standard_doc`

| 列 | 说明 |
|----|------|
| `doc_code` | 业务主键，如 `gcp-2020` |
| `title` / `short_title` / `doc_number` / `issuer` | 展示与出处 |
| `category_id` | FK → category |
| `priority` | primary / secondary / reference |
| `status` | active / upcoming / reference |
| `status_label` | 中文状态 |
| `effective_from` / `effective_until` | 效力窗口 |
| `project_role` | 在本项目中的作用 |
| `local_file` / `local_file_label` | 本地归档路径 |
| `extra` | JSONB |

#### `standard_clause`

| 列 | 说明 |
|----|------|
| `doc_id` | FK |
| `clause_ref` | 如 `第二十三条（六）` |
| `summary` | 摘要 |
| `sort_order` | |

#### `rubric_set`

| 列 | 说明 |
|----|------|
| `set_code` | 如 `RUBRIC-S1-MVP` |
| `scene_id` | FK |
| `version` | 如 `1.0.0-mvp` |
| `pass_rule` | 通过规则文案 |
| `status` | draft / active / retired |

#### `rubric_item`

| 列 | 说明 |
|----|------|
| `set_id` | FK |
| `item_code` | `IC-P04`（集内唯一） |
| `layer` | L1/L2/L3 |
| `item_type` | process / content / prohibition |
| `title` / `pass_criteria` / `fail_criteria` | |
| `hard_fail` / `weight` / `enabled_mvp` | |
| `binds_symptom` | 是否依赖病例事实 |
| `lay_title` / `lay_explain` | 可冗余存白话，或走 content 表 |

#### `rubric_item_source`

| 列 | 说明 |
|----|------|
| `rubric_item_id` | FK |
| `doc_code` | 可空若仅内部引用 |
| `clause_ref` | 条款 |
| `note` | |
| `source_raw` | 原始串，如 `gcp-2020:第二十三条（六）` |

#### `rubric_item_binding`

| 列 | 说明 |
|----|------|
| `rubric_item_id` + 可选 `case_id` | 全局或按病例 |
| `binding_type` | symptom / risk / benefit / visit_item |
| `target_code` | `SX-T2DM-01` |
| `enabled` | **病例未 reviewed 前必须 false** |

### 4.2 M2 病例（摘要）

详见 [`病例设计文档.md`](./病例设计文档.md)。要点：

- `case_package.case_code` + `version` + `data_status`
- `case_symptom.symptom_code` 稳定、可被评分绑定
- `case_forbidden` 供 AI grounding 使用

### 4.3 M3 会话与反馈

#### `training_session`

| 列 | 说明 |
|----|------|
| `case_id` / `case_version` | 锁定病例版本 |
| `persona_id` / `scene_id` | |
| `rubric_set_id` / `rubric_version` | 锁定评分表版本 |
| `trainee_user_id` / `trainee_ref` | 账号或匿名标记 |
| `status` | in_progress / completed / abandoned |
| `patient_prompt_version_id` | 开练时患者 prompt 版本 |
| `started_at` / `ended_at` | |

#### `session_message`

| 列 | 说明 |
|----|------|
| `turn_index` | 从 0 递增 |
| `role` | trainee / patient / system / coach |
| `content` | 文本 |
| `ai_generation_id` | 若本条由模型生成则 FK |
| `client_meta` | 可选：耗时、语音 ASR 等 |

#### `score_result`

| 列 | 说明 |
|----|------|
| `session_id` + `item_code` | UNIQUE |
| `verdict` | pass / fail / uncertain / skipped |
| `evidence_spans` | `[{turn_index, quote, start, end}]` |
| `comment` / `source_refs` | |
| `scorer_type` | rule / llm / hybrid |
| `ai_generation_id` | 若 LLM 评分 |

#### `feedback_report`

| 列 | 说明 |
|----|------|
| `session_id` | UNIQUE 一期一份 |
| `overall_pass` | bool |
| `summary` | 总述 |
| `improvements` | JSONB 建议列表 |
| `disclaimer` | 教学模拟声明 |
| `payload` | JSONB 完整报告快照 |

### 4.4 M4 AI 存储 ★

#### `ai_model_profile`

| 列 | 说明 |
|----|------|
| `profile_code` | 如 `patient-chat-default` |
| `provider` | openai / azure / local / … |
| `model_name` | |
| `params` | JSONB：temperature、max_tokens 等 |
| `secret_ref` | 密钥引用名，**不存明文 API Key** |
| `is_active` | |

#### `ai_prompt_template` + `ai_prompt_version`

| 用途 `purpose` | 说明 |
|----------------|------|
| `patient_system` | AI 患者系统约束（注入病例事实） |
| `patient_turn` | 单轮患者回复 |
| `scorer` | 评分引擎 |
| `feedback` | 反馈生成 |
| `safety` | 安全/违规检测（可选） |

`ai_prompt_version` 存 `template_body`、`input_schema`、`version`、`status`。

#### `ai_agent_run`

一次「患者回复」或「整段评分」的编排头：

| 列 | 说明 |
|----|------|
| `session_id` | 可空（离线批处理） |
| `run_type` | patient_reply / score_session / feedback |
| `status` | running / succeeded / failed |
| `started_at` / `ended_at` | |

#### `ai_generation_log`

| 列 | 说明 |
|----|------|
| `run_id` | FK agent_run |
| `profile_id` / `prompt_version_id` | |
| `request_payload` | JSONB：消息列表或摘要（注意脱敏） |
| `response_text` | 模型原文 |
| `response_raw` | JSONB 完整响应 |
| `token_usage` | JSONB |
| `latency_ms` | |
| `error_message` | |
| `created_at` | |

#### `ai_grounding_check`

对患者生成做事实校验：

| 列 | 说明 |
|----|------|
| `generation_id` | FK |
| `check_type` | forbidden_symptom / forbidden_claim / contradiction / … |
| `passed` | bool |
| `detail` | JSONB：命中的 forbidden_code、抽取实体等 |
| `action` | allow / regenerate / replace_with_fallback |

**原则**：病例 `forbidden` + `symptoms(present=false)` 是校验输入；校验失败应记录，并优先再生或回退话术，而不是默默放行。

---

## 5. 索引与约束（全库要点）

| 对象 | 建议 |
|------|------|
| `standard_doc(doc_code)` | UNIQUE |
| `rubric_item(set_id, item_code)` | UNIQUE |
| `case_package(case_code)` | UNIQUE；默认病例部分唯一索引 |
| `case_symptom(case_id, symptom_code)` | UNIQUE |
| `training_session(case_id, started_at DESC)` | 历史列表 |
| `session_message(session_id, turn_index)` | UNIQUE |
| `score_result(session_id, item_code)` | UNIQUE |
| `ai_generation_log(session_id via run, created_at)` | 审计检索 |
| `ai_prompt_version(template_id, version)` | UNIQUE |

软删除：标准/病例/评分集用 `status` / `data_status`，**不删**已被会话引用的版本行（或会话内已快照版本号即可）。

---

## 6. JSON 配置 ↔ 表映射（一期）

| 文件 | 主要落入 |
|------|----------|
| `standards-registry.json` | `standard_*` |
| `scoring-rubric.json` | `rubric_set` / `rubric_item` / `rubric_item_source` |
| `terminology.json` | `terminology` |
| `scenes.json` | `scene` |
| `cases-index.json` + `cases/*.json` | M2 全套 |
| `project-guide.json` 等 | M5 content_* |
| （运行时产生） | M3 + M4 |

导入要求：按业务码幂等 upsert；导入写入 `data_import_log`。

---

## 7. 双底座咬合（库内如何表达）

```text
rubric_item (IC-C04 说明风险)
    │ rubric_item_source → standard_doc/clause（GCP 第二十四条…）
    │
    └── rubric_item_binding (enabled=true 仅当 case reviewed)
            → case_risk_point.risk_code (RK-T2DM-*)
            → 或 case_symptom.symptom_code（追问类项）
```

| 条件 | 行为 |
|------|------|
| 病例 `placeholder` / `draft` | binding.`enabled=false`；IC 通用项仍可评 |
| 病例 `reviewed` / `active` | 允许启用症状/风险硬绑定 |
| 会话进行中 | 只读开练时锁定的 case_version + rubric_version |

---

## 8. AI 数据生命周期

1. **配置期**：维护 model_profile、prompt_version（评审后 active）。  
2. **对话期**：每条患者回复 → `ai_agent_run` + `generation_log` + 可选 `grounding_check` → 通过后写入 `session_message`。  
3. **评分期**：整段会话 → scorer prompt → 多条 `score_result`；日志挂同一 `agent_run`。  
4. **反馈期**：生成 `feedback_report`，可再记一条 generation。  
5. **保留策略**：教学日志默认保留；若含敏感自由文本，可配置保留天数与脱敏任务（预留，一期不做自动删除）。

**禁止**：把 API Key 写入数据库明文；把真实患者病历写入本库。

---

## 9. 一期落地路径

| 阶段 | 做法 |
|------|------|
| 现在 | JSON 驱动前端与联调；执行 `db/schema.sql` 建空库 |
| 评分/患者 Agent 联调 | Session/Message/Score/AI log 先写入 DB 或 SQLite |
| 多病例上线 | 导入标准+病例主数据；运行时全走 DB |
| 密钥 | 环境变量 / 密钥管理；`secret_ref` 仅存引用名 |

---

## 10. 安全与合规

- 教学库：无真实姓名/身份证/病历号。  
- `feedback_report.disclaimer` 与病例 `disclaimer` 必填语义。  
- `is_placeholder` 方案与 `data_status=placeholder` 病例不得当真实招募材料外发。  
- AI 日志中的 prompt 若含完整病例，访问需授权（教员/开发角色预留）。

---

## 11. 演进策略

1. 加表/加 JSONB 列优先于重命名业务码。  
2. 评分集用新 `rubric_set.version`，不覆盖旧集（旧会话可复现）。  
3. Prompt 只增 `ai_prompt_version`，会话记录所用 version id。  
4. 多模态（语音）后期：`session_message` 增加 `media_asset_id`，另表存对象存储引用。  
5. 多租户后期：加 `org_id`，本期不加以免过度设计。

---

## 12. 与业务文档的对照自检

- [ ] 标准设计中的注册项、条款、评分字段是否都有表？  
- [ ] 病例设计中的症状/风险/禁止编造/人设是否都有表？  
- [ ] 架构中的「会话 → 评分 → 反馈」是否可落库？  
- [ ] AI 患者约束与评分调用是否可审计（prompt 版本 + generation log + grounding）？  
- [ ] 双底座咬合是否仅通过 binding，且有 enabled 开关？

---

## 修订记录

| 日期 | 说明 |
|------|------|
| 2026-08-23 | 初稿（仅病例侧，不完整） |
| 2026-08-23 | **重写为全项目库**：标准、评分、病例、会话、反馈、AI 存储、内容层 |
